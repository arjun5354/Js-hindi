"use strict" // treat all js code as newer version -> by deafult it is used by compilar -> because compilar is smart
// alert("Hello") // It will through error because Hum abhi nodejs ke andar hai [but ye chrome main inspect karne pe chale ga kewke Uha pai jo likha jata hai bo document ke andar likha jata hai -> js ka engin browser ke andar chupa hua hai]
// Aleart hai node ke andar but different syntax main jo hum bad main karenge

console.log(3+3) ;console.log(23) // If we not use ; then code will through error


let name="Arjun"
let age=19
let isLoggedIn=false;
let state=null; //**** Ex -> getting temparature from browser but currently browser is not working perfectly if it returns 0 then it will be a valid temparature but if it returns null then we understand that yes there is something problemetic..
// data type ->primitive

// number=>2 to the power 53
// bigint
// string=>""
// boolean=> true/false
// null=>standalone value
// undefined =>
// symbol=> Identify unique  component
 
// Object
console.log(typeof null); // object
console.log(typeof undefined); // undefined
